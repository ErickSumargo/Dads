rootProject.name = "Bael"

def modules = [
    "app",
    "annotation",
    "processor",
    "feature_sample",
    "lib_presentation"
]

modules.forEach { module ->
    include ":$module"

    if (module.contains("feature")) {
        project(":$module").projectDir = new File("features/$module")
    } else if (module.contains("lib")) {
        project(":$module").projectDir = new File("libs/$module")
    }
}
