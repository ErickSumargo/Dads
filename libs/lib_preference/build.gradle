apply from: "$rootDir/libs/lib.gradle"

dependencies {
    // datastore
    implementation "androidx.datastore:datastore-preferences:$deps.datastore"
}

dependencies {
    // libs
    implementation project(":lib_threading")
}
