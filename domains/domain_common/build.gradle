apply from: "$rootDir/domains/domain.gradle"

def plugins = [
    "kotlin-kapt",
    "dagger.hilt.android.plugin"
]

plugins.forEach { plugin ->
    apply plugin: plugin
}

def deps = [
    "dagger"
]

deps.forEach { dep ->
    apply from: "$rootDir/deps/${dep}.gradle"
}
